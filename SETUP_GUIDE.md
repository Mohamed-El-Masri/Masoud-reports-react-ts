# 🚀 دليل تشغيل تطبيق التقارير

## المرحلة الحالية: ✅ تم إنشاء الهيكل الأساسي بنجاح

---

## 📦 خطوات التثبيت والتشغيل

### 1. انتقل إلى مجلد التطبيق
```powershell
cd "c:\Users\a\Desktop\Masoud Copany Tec\reports-app"
```

### 2. تثبيت الحزم (Dependencies)
```powershell
npm install
```

### 3. نسخ ملف البيئة
```powershell
Copy-Item .env.example .env
```

### 4. تشغيل التطبيق في بيئة التطوير
```powershell
npm run dev
```

سيعمل التطبيق على: **http://localhost:3001**

---

## ✅ ما تم إنجازه

### المرحلة 1: إعداد البيئة ✅
- ✅ إنشاء مشروع Vite + React + TypeScript
- ✅ إعداد TailwindCSS مع نظام تصميم كامل
- ✅ إعداد ESLint & Prettier
- ✅ إعداد Path aliases (@components, @pages, إلخ)
- ✅ إعداد i18n للترجمة (عربي/إنجليزي)

### المرحلة 2: نظام التصميم والمكونات ✅
- ✅ Design tokens (ألوان، خطوط، مسافات)
- ✅ دعم RTL كامل
- ✅ مكونات UI الأساسية:
  - Button
  - Card
  - Loading & Skeleton
  - Badge
- ✅ Layout components (Header, Sidebar)

### المرحلة 3: النظام الأساسي ✅
- ✅ API Client مع Axios
- ✅ React Query للـ caching
- ✅ Zustand Stores (Auth, UI)
- ✅ TypeScript Types كاملة
- ✅ Custom Hooks (useDashboard, useReports, useData)
- ✅ React Router مع Protected Routes

### المرحلة 4: الصفحات الأساسية ✅
- ✅ صفحة Login
- ✅ Dashboard كامل مع:
  - 6 KPIs
  - 4 رسوم بيانية
  - قسم التنبيهات
- ✅ صفحة تقرير تقدم المشاريع (نموذج كامل)
- ✅ هياكل باقي صفحات التقارير (6 صفحات)
- ✅ صفحة Analytics (جاهزة للبناء)
- ✅ صفحة Timeline (جاهزة للبناء)

---

## 📂 هيكل المشروع المنجز

```
reports-app/
├── src/
│   ├── app/
│   │   ├── App.tsx ✅
│   │   └── Layout.tsx ✅
│   ├── pages/
│   │   ├── Login/ ✅
│   │   ├── Dashboard/ ✅
│   │   │   └── components/ ✅
│   │   ├── Reports/
│   │   │   ├── ProjectProgress/ ✅
│   │   │   ├── EquipmentUtilization/ ✅
│   │   │   ├── FuelConsumption/ ✅
│   │   │   ├── Certificates/ ✅
│   │   │   ├── QSSummary/ ✅
│   │   │   ├── DirectCosts/ ✅
│   │   │   └── Profitability/ ✅
│   │   ├── Analytics/ ✅
│   │   └── Timeline/ ✅
│   ├── components/
│   │   ├── ui/ ✅
│   │   └── layout/ ✅
│   ├── hooks/ ✅
│   ├── services/api/ ✅
│   ├── stores/ ✅
│   ├── types/ ✅
│   ├── utils/ ✅
│   └── i18n/ ✅
├── package.json ✅
├── vite.config.ts ✅
├── tailwind.config.js ✅
├── tsconfig.json ✅
└── README.md ✅
```

---

## 🎨 المميزات المتوفرة

### 1. نظام تصميم احترافي
- ألوان الشركة (أزرق، رملي، أرضي)
- دعم Dark Mode كامل
- دعم RTL كامل للعربية
- مكونات قابلة لإعادة الاستخدام

### 2. إدارة الحالة
- Zustand للحالة المحلية
- React Query لحالة الخادم
- LocalStorage persistence

### 3. الأمان
- JWT Authentication
- Protected Routes
- Auto token refresh
- Secure API calls

### 4. الأداء
- Code splitting
- Lazy loading
- Optimized bundling
- Fast refresh

---

## 🔄 المراحل القادمة

### المرحلة 5: إكمال صفحات التقارير ⏳
- [ ] تنفيذ تقرير استخدام المعدات
- [ ] تنفيذ تقرير استهلاك الوقود
- [ ] تنفيذ تقرير المستخلصات
- [ ] تنفيذ تقرير الحصر
- [ ] تنفيذ تقرير التكاليف المباشرة
- [ ] تنفيذ تقرير الربحية
- [ ] إضافة export functionality (PDF, Excel, CSV)

### المرحلة 6: Analytics & Timeline ⏳
- [ ] بناء صفحة Analytics
- [ ] فلاتر ديناميكية
- [ ] بحث متقدم
- [ ] بناء صفحة Timeline
- [ ] عرض الأحداث الزمنية

### المرحلة 7: التحسينات ⏳
- [ ] إضافة Unit Tests
- [ ] إضافة E2E Tests
- [ ] تحسين الأداء
- [ ] Accessibility audit
- [ ] Production build optimization

---

## 📖 الاستخدام

### تسجيل الدخول (Development)
يمكنك استخدام أي بريد إلكتروني وكلمة مرور للتجربة حالياً.

### التنقل
- Dashboard: لوحة القيادة الرئيسية
- التقارير: 7 تقارير مختلفة
- التحليلات: تحليلات متقدمة
- الخط الزمني: عرض الأحداث

---

## 🛠️ أوامر مفيدة

```powershell
# تشغيل في بيئة التطوير
npm run dev

# بناء للإنتاج
npm run build

# معاينة الـ build
npm run preview

# فحص الكود
npm run lint

# تنسيق الكود
npm run format

# فحص الأنواع
npm run type-check

# تشغيل الاختبارات
npm run test
```

---

## 📝 ملاحظات مهمة

1. **Mock Data**: حالياً التطبيق يستخدم بيانات وهمية. ستحتاج لربطه بـ Backend API الحقيقي.

2. **API Integration**: جميع API calls جاهزة في `src/services/api/`, فقط قم بتحديث الـ endpoints.

3. **Authentication**: نظام المصادقة جاهز، يحتاج فقط لربطه بـ API الحقيقي.

4. **Environment Variables**: لا تنسى تحديث ملف `.env` بالقيم الصحيحة.

5. **RTL Support**: التطبيق يدعم RTL بشكل كامل، كل CSS يستخدم logical properties.

---

## 🎯 الخطوات التالية الموصى بها

1. **تشغيل التطبيق**:
   ```powershell
   cd "c:\Users\a\Desktop\Masoud Copany Tec\reports-app"
   npm install
   npm run dev
   ```

2. **استكشاف الكود**: افتح التطبيق في المتصفح وتفقد الصفحات المختلفة.

3. **تخصيص**: عدّل الألوان والنصوص حسب احتياجك.

4. **إكمال التقارير**: ابدأ بتنفيذ باقي صفحات التقارير.

5. **ربط الـ Backend**: عندما يكون Backend جاهز، قم بتحديث API endpoints.

---

## 💡 نصائح للتطوير

- استخدم الـ React Query DevTools لمراقبة الـ cache
- استخدم الـ Redux DevTools لمراقبة الـ Zustand stores
- التزم بـ TypeScript strict mode
- اتبع معايير التسمية الموجودة
- اكتب تعليقات واضحة

---

## 🤝 المساعدة

إذا واجهت أي مشكلة:
1. تحقق من console للأخطاء
2. تحقق من Network tab للـ API calls
3. راجع الـ README في كل مجلد
4. اطلب المساعدة!

---

**🎉 التطبيق جاهز للتطوير! ابدأ الآن!**
